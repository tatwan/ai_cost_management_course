# Next Steps

> **Purpose:** the running backlog — what's intentionally left to do, roughly
> ordered by priority. This is where you look *before* starting work, to pick
> up the next task, and where you add a line the moment you think "someone
> should do X" but you're not doing it right now.
>
> **How to use it:** check an item off (`- [x]`) and move it to `log.md` with a
> dated entry when it's actually done — don't just delete it silently. Keep
> items short and concrete ("re-check rate card against provider pricing
> pages") rather than vague ("improve costs section").

---

## Backlog

- [ ] Re-verify `coursekit.PRICES` against live provider pricing pages before
      the next cohort — the rate card is dated (see `current_state.md`).
- [ ] Decide whether to install `llmlingua` by default (`compress` extra) or
      keep Lesson 3's compression cell opt-in.
- [ ] Consider adding a `progress/` entry template snippet to
      `notebooks/README.md` so students see the pattern referenced from the
      lesson material too, not just from `AGENTS.md`.

## Someday / not scheduled

- [ ] Explore whether Lesson 6 (routing cascade) should pull live success-rate
      data instead of the illustrative UCCI 2026 figure cited in the README.
